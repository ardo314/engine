[package]
name = "engine-ecs"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-schema = { path = "../engine-schema" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[package]
name = "engine_component"
version = "0.1.0"
edition = "2024"
description = "Component trait, type-erased storage, and ECS primitives for the distributed engine."

[dependencies]
serde = { workspace = true }
rmp-serde = { workspace = true }
thiserror = { workspace = true }
